//Given an array of strings, return only the words that begin with the letter "t"

var words = ["super", "tasteful", "guitar", "truck", "time", "plant"]

for (var i = 0; i < words.length; i++) {

console.log(words[i].startsWith("t"));
}